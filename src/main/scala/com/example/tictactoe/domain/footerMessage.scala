package com.example.tictactoe.domain

enum ConfirmFooterMessage:
  case Empty
  case InvalidCommand

enum GameFooterMessage:
  case Empty
  case InvalidCommand
  case FieldOccupied

enum MenuFooterMessage:
  case Empty
  case InvalidCommand
